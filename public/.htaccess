RewriteEngine On

# Si la ruta pedida no corresponde a un archivo o directorio real
# (assets, imágenes, etc.), se enruta a index.php como front controller.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
